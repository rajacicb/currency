<Window x:Class="CurrencyConverter.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Background="#00ACDC"     
        Title="{Binding Title}" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="65"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Margin="20,0,0,0" Text="Currency Converter" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="36"/>
        <ContentControl prism:RegionManager.RegionName="ContentRegion" Margin="20" Grid.Row="2" />
    </Grid>
</Window>
